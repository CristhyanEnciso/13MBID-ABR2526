clean:
  drop_null_threshold: 0.2
  drop_columns: ["default"]
select:
  keep_columns:  # para fijarlas explícitamente
    - age
    - job
    - marital
    - education
    - contact
    - month
    - day_of_week
    - campaign
    - previous
    - poutcome
    - emp.var.rate
    - cons.price.idx
    - cons.conf.idx
    - euribor3m
    - nr.employed
    - y
features:
  derive_prev_contact_from_pdays: true
  pdays_never_value: 999
format:
  target_name: y
  positive_label: "yes"
  order_target_last: true
